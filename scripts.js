const lineColors = {
    green: "#44bb66",
    red: "#bb4466",
    blue: "#4466bb"
}

const config = {
    displayModeBar: false,
    responsive: true
}

const plotDiv = document.getElgementById("vis1");
const plotDiv = document.getElgementById("vis2");

Plotly.d3.csv("csv", function(rows){

});